# Copy this to secrets.toml (never commit secrets.toml — it's in .gitignore)
# On Streamlit Community Cloud, paste these values in:
#   App settings → Secrets

# URL of your deployed Render backend (no trailing slash)
API_BASE = "https://your-app-name.onrender.com/api/v1/sessions"
